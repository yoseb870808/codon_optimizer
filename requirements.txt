# Core dependencies
biopython>=1.83
pandas>=2.1
numpy>=1.24
openpyxl>=3.1
pyyaml>=6.0

# mRNA structure analysis (mandatory — pipeline imports RNA at startup)
# Install via pip (Windows wheels available for Python 3.10–3.13):
#   pip install ViennaRNA
# If the wheel is unavailable for your platform, use conda:
#   conda install -c bioconda viennarna
ViennaRNA>=2.6

# Testing
pytest>=7.4
