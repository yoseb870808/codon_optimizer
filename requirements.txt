# Core dependencies
biopython>=1.83
pandas>=2.1
numpy>=1.24
openpyxl>=3.1
pyyaml>=6.0

# mRNA structure analysis (required)
# Install ViennaRNA via conda if pip fails:
#   conda install -c bioconda viennarna
ViennaRNA>=2.6

# Testing
pytest>=7.4
